package com.syncone.health.domain.model.enums

enum class UrgencyLevel {
    NORMAL,   // Standard health query
    URGENT,   // Needs attention within hours
    CRITICAL  // Immediate intervention required
}
